import { DepositButton } from "pampalo";

export const Default = () => (
  <div style={{ width: 360 }}>
    <DepositButton onClick={() => {}} />
  </div>
);

export const NarrowColumn = () => (
  <div style={{ width: 220 }}>
    <DepositButton onClick={() => {}} />
  </div>
);
