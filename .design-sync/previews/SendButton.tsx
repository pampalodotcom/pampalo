import { SendButton } from "pampalo";

export const Default = () => (
  <div style={{ width: 360 }}>
    <SendButton onClick={() => {}} />
  </div>
);

export const NarrowColumn = () => (
  <div style={{ width: 220 }}>
    <SendButton onClick={() => {}} />
  </div>
);
