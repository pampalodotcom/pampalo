import { ReceiveButton } from "pampalo";

export const Default = () => (
  <div style={{ width: 360 }}>
    <ReceiveButton onClick={() => {}} />
  </div>
);

export const NarrowColumn = () => (
  <div style={{ width: 220 }}>
    <ReceiveButton onClick={() => {}} />
  </div>
);
